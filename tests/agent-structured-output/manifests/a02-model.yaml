apiVersion: ark.mckinsey.com/v1alpha1
kind: Model
metadata:
  name: structured-output-test-model
  namespace: ($namespace)
spec:
  type: azure
  model:
    value: gpt-4.1-mini
  config:
    azure:
      baseUrl:
        valueFrom:
          secretKeyRef:
            name: structured-output-test-secret
            key: url
      apiKey:
        valueFrom:
          secretKeyRef:
            name: structured-output-test-secret
            key: token
      apiVersion:
        value: "2024-12-01-preview"