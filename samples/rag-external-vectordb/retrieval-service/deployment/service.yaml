apiVersion: v1
kind: Service
metadata:
  name: rag-retrieval-http
spec:
  selector:
    app: rag-retrieval-http
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP

