apiVersion: ark.mckinsey.com/v1alpha1
kind: Agent
metadata:
  name: invalid-agent-tool-name-and-selector
spec:
  prompt: Test agent
  modelRef:
    name: nonexistent-model
  tools:
  - type: custom
    name: test-tool
    labelSelector:  # Both name and labelSelector specified - should fail admission
      matchLabels:
        type: test