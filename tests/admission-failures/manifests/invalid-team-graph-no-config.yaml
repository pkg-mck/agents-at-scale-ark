apiVersion: ark.mckinsey.com/v1alpha1
kind: Team
metadata:
  name: invalid-team-graph-no-config
spec:
  strategy: graph  # Graph strategy requires graph config - should fail admission
  members:
  - type: agent
    name: agent1
  - type: agent
    name: agent2