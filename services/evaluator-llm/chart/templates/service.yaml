apiVersion: v1
kind: Service
metadata:
  name: {{ include "evaluator-llm.fullname" . }}
  labels:
    {{- include "evaluator-llm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "evaluator-llm.selectorLabels" . | nindent 4 }}