apiVersion: ark.mckinsey.com/v1alpha1
kind: Agent
metadata:
  name: agent-no-model
spec:
  prompt: |
    You are a test agent that should automatically use the default model.
    Respond with "Using default model" to confirm you are working.