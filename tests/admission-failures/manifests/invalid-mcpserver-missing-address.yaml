apiVersion: ark.mckinsey.com/v1alpha1
kind: MCPServer
metadata:
  name: invalid-mcpserver-missing-address
spec:
  # Missing required address field - should fail admission
  headers:
  - name: Authorization
    value:
      value: Bearer token