apiVersion: v1
kind: Service
metadata:
  name: {{ include "ark-cluster-memory.fullname" . }}
  labels:
    {{- include "ark-cluster-memory.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ark-cluster-memory.selectorLabels" . | nindent 4 }}