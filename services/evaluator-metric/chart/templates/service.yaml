apiVersion: v1
kind: Service
metadata:
  name: {{ include "evaluator-metric.fullname" . }}
  labels:
    {{- include "evaluator-metric.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "evaluator-metric.selectorLabels" . | nindent 4 }}