apiVersion: v1
kind: Service
metadata:
  name: {{ include "ark-evaluator.fullname" . }}
  labels:
    {{- include "ark-evaluator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ark-evaluator.selectorLabels" . | nindent 4 }}