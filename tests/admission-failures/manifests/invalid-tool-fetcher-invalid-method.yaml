apiVersion: ark.mckinsey.com/v1alpha1
kind: Tool
metadata:
  name: invalid-tool-fetcher-invalid-method
spec:
  type: fetcher
  inputSchema:
    type: object
    properties:
      param:
        type: string
  fetcher:
    url: https://example.com
    method: INVALID  # Invalid HTTP method - should fail admission