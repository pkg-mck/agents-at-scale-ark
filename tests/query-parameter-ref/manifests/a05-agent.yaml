apiVersion: ark.mckinsey.com/v1alpha1
kind: Agent
metadata:
  name: test-agent
spec:
  modelRef:
    name: test-model
  prompt: |
    Your name is {{.agent_name}}.
  parameters:
    - name: agent_name
      valueFrom:
        queryParameterRef:
          name: your_name