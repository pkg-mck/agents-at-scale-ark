apiVersion: ark.mckinsey.com/v1alpha1
kind: Query
metadata:
  name: invalid-query-both-evaluators-and-selector
spec:
  input: Test query
  targets:
  - type: agent
    name: nonexistent-agent
  evaluators:  # Cannot specify both evaluators and evaluatorSelector - should fail admission
  - name: nonexistent-evaluator
  evaluatorSelector:
    matchLabels:
      type: test