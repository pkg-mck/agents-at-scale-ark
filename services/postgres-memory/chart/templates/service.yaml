apiVersion: v1
kind: Service
metadata:
  name: {{ include "postgres-memory.fullname" . }}
  labels:
    {{- include "postgres-memory.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "postgres-memory.selectorLabels" . | nindent 4 }}