apiVersion: ark.mckinsey.com/v1alpha1
kind: Evaluator
metadata:
  name: invalid-evaluator-invalid-model
spec:
  address:
    value: http://localhost:8080
  parameters:
    - name: model.name
      value: nonexistent-model  # Invalid model reference - should fail admission