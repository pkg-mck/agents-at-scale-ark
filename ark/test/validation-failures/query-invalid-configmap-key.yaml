apiVersion: ark.mckinsey.com/v1alpha1
kind: Query
metadata:
  name: test-query-invalid-configmap-key
spec:
  input: "Please analyze this data: {{.missing_key}}"
  parameters:
    - name: missing_key
      valueFrom:
        configMapKeyRef:
          name: shared
          key: nonexistent-key
  targets:
    - type: agent
      name: test-agent