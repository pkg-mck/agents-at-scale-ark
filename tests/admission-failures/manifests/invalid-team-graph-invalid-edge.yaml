apiVersion: ark.mckinsey.com/v1alpha1
kind: Team
metadata:
  name: invalid-team-graph-invalid-edge
spec:
  strategy: graph
  members:
  - type: agent
    name: agent1
  - type: agent
    name: agent2
  graph:
    edges:
    - from: agent1
      to: nonexistent-agent  # Edge references non-member - should fail admission