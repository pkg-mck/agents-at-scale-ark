apiVersion: ark.mckinsey.com/v1alpha1
kind: Model
metadata:
  name: test-model-missing-url
  namespace: default
spec:
  type: openai
  model:
    value: "gpt-4"
  # Missing required config.openai.baseUrl and config.openai.apiKey - should cause validation failure