apiVersion: ark.mckinsey.com/v1alpha1
kind: Agent
metadata:
  name: test-agent-invalid-secret
spec:
  prompt: "You are a helpful assistant. {{.missing_secret}}"
  parameters:
    - name: missing_secret
      valueFrom:
        secretKeyRef:
          name: nonexistent-secret
          key: some-key
  tools: []