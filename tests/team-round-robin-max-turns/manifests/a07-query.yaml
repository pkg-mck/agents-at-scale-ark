apiVersion: ark.mckinsey.com/v1alpha1
kind: Query
metadata:
  name: max-turns-team-query
spec:
  input: "Provide a brief solution for improving team communication. Keep responses short."
  targets:
  - type: team
    name: round-robin-max-turns-team