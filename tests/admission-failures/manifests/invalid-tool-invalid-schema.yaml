apiVersion: ark.mckinsey.com/v1alpha1
kind: Tool
metadata:
  name: invalid-tool-invalid-schema
spec:
  type: fetcher
  inputSchema:
    type: invalid-type  # Invalid JSON schema type - should fail admission
    properties:
      url:
        type: string
  fetcher:
    url: https://example.com