apiVersion: ark.mckinsey.com/v1alpha1
kind: Agent
metadata:
  name: test-agent-invalid-configmap
spec:
  prompt: "You are a helpful assistant. {{.missing_config}}"
  parameters:
    - name: missing_config
      valueFrom:
        configMapKeyRef:
          name: nonexistent-configmap
          key: some-key
  tools: []