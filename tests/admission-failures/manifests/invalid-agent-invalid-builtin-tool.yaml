apiVersion: ark.mckinsey.com/v1alpha1
kind: Agent
metadata:
  name: invalid-agent-invalid-builtin-tool
spec:
  prompt: Test agent
  modelRef:
    name: nonexistent-model
  tools:
  - type: built-in
    name: invalid-builtin  # Invalid built-in tool name - should fail admission