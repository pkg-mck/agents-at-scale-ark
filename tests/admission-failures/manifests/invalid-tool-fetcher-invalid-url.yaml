apiVersion: ark.mckinsey.com/v1alpha1
kind: Tool
metadata:
  name: invalid-tool-fetcher-invalid-url
spec:
  type: fetcher
  inputSchema:
    type: object
    properties:
      param:
        type: string
  fetcher:
    url: "not-a-valid-url"  # Invalid URL format - should fail admission
    method: GET