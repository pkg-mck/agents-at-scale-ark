apiVersion: ark.mckinsey.com/v1alpha1
kind: Model
metadata:
  name: weather-default-model
spec:
  type: azure
  model:
    value: gpt-4.1-mini
  config:
    azure:
      baseUrl:
        value: ($azure.url)
      apiKey:
        valueFrom:
          secretKeyRef:
            name: weather-model-token
            key: token
      apiVersion:
        value: "2024-12-01-preview"