apiVersion: ark.mckinsey.com/v1alpha1
kind: Agent
metadata:
  name: invalid-agent-builtin-with-selector
spec:
  prompt: Test agent
  modelRef:
    name: nonexistent-model
  tools:
  - type: built-in
    name: noop
    labelSelector:  # Built-in tools cannot use labelSelector - should fail admission
      matchLabels:
        type: test